# Dependencias
node_modules/

# Secretos - NUNCA deben subirse (este repo es publico)
.env
.env.local
.env.*.local
*.keystore
*.jks
google-services.json

# Expo / React Native
.expo/
dist/
web-build/
*.orig.*
expo-env.d.ts

# Codigo nativo generado por prebuild (lo regenera EAS)
/android/
/ios/

# Logs y temporales
*.log
npm-debug.*
yarn-error.*
.DS_Store
Thumbs.db

# Ajustes locales de Claude Code (personales, no del proyecto)
.claude/settings.local.json

# Expo / RN adicionales (del template oficial)
*.tsbuildinfo
.metro-health-check*
.kotlin/
*.p12
*.p8
*.pem
*.mobileprovision
yarn-debug.*

# Temporales del CLI de Supabase
supabase/.temp/

# Capturas de terceros usadas como referencia: el repo es publico
assets/onboarding/
assets/inicio/

# Reseñas de terceros y capturas personales: no van en un repo público
reseñas/
bugs/
.claude/settings.local.json
