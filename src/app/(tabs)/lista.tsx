import { PantallaVacia } from '@/components/pantalla-vacia';

export default function Pantalla() {
  return (
    <PantallaVacia
      titulo="Tu lista está vacía"
      texto="Añade ingredientes a mano o genera la lista desde tus recetas guardadas."
    />
  );
}
