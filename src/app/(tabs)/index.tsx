import { PantallaVacia } from '@/components/pantalla-vacia';

export default function Pantalla() {
  return (
    <PantallaVacia
      titulo="Tu biblioteca está vacía"
      texto="Comparte un reel desde Instagram o TikTok, o pega un enlace, y Recetifia lo convierte en una receta."
    />
  );
}
