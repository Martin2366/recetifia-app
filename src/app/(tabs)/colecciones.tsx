import { PantallaVacia } from '@/components/pantalla-vacia';

export default function Pantalla() {
  return (
    <PantallaVacia
      titulo="Sin colecciones todavía"
      texto="Agrupa tus recetas en libros: postres, almuerzos rápidos, lo que quieras."
    />
  );
}
